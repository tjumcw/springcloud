server:
  port: 8761
eureka:
  client:
    register-with-eureka: false # 是否在为服务中心注册自己（否，只注册他人）
    fetch-registry: false # 是否同步其他注册中心配置
    service-url:
      defaultZone: http://localhost:8761/eureka
